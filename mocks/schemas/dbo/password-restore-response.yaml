title: Password restore response
required:
  - confirmationId
  - restorePasswordToken
type: object
properties:
  confirmationId:
    type: string
    description: Id of user password restore attempt
  restorePasswordToken:
    type: string
    description: Token for restore password process
